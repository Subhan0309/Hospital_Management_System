<%= render partial: 'shared/table_view', locals: {
  title: 'Doctors',
  collection: @doctors,
  headers: ['Name', 'Email', 'Gender','View Details'],
  fields: ['name', 'email', 'gender'],
  resource_class: Doctor,
  new_path: new_doctor_path,
  edit_path: ->(item) { edit_doctor_path(item) },
  destroy_path: ->(item) { doctor_path(item) },
  comment_path: ->(item) {doctor_comments_path(item)},
  show_appointments_path: ->(item) {user_appointments_path(item)},
  add_comment_path: ->(item) { doctor_comments_path(item) } # Path for adding comments
} %>
