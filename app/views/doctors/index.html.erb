<div class="container-fluid">
  <div class="row flex-nowrap">
    <!-- Render the sidebar partial -->
    <%= render 'shared/sidebar' %>

    <div class="col py-3">
     

      
      <!-- Flex container for header and button -->
      <div class="d-flex flex-row justify-content-between align-items-center mb-3">
        <!-- Heading -->
        <h1 class="text-primary mb-0">Doctors</h1>
        
        <!-- Button to create a new Doctor -->
        <%= link_to 'Add New Doctor', new_doctor_path, class: 'btn btn-primary' %>
      </div>

      <!-- Doctor table -->
      <% if @doctors.any? %>
        <div class="table-responsive mt-5">
          <table class="table table-striped">
            <thead>
              <tr>
                <th>#</th>
                <th>Name</th>
                <th>Email</th>
                <th>Role</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% @doctors.each_with_index do |doctor, index| %>
                <tr>
                  <td><%= index + 1 %></td>
                  <td><%= doctor.name %></td>
                  <td><%= doctor.email %></td>
                  <td><%= doctor.role.capitalize %></td>
                  <td>
                  <!-- Edit Button with Icon -->
                      <%= link_to edit_doctor_path(doctor), class: 'btn btn-success btn-sm' do %>
                        <i class="bi bi-pencil-square"></i> 
                      <% end %>

                      <!-- Delete Button with Icon -->
                      <%= button_to doctor_path(doctor), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' do %>
                        <i class="bi bi-trash"></i> 
                      <% end %>  
                 </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <div class="alert alert-warning" role="alert">
          No Doctor found.
        </div>
      <% end %>
    </div>
  </div>
</div>
