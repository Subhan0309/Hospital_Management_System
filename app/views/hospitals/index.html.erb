<div class="container d-flex flex-row align-items-center justify-content-between my-4 w-100">
  <h1 class="text-center mb-4 text-primary">Owner</h1>
  <div class="text-center mb-4">
    <%= link_to 'Create New Hospital', new_user_registration_path, class: 'btn btn-primary btn-lg' %>
  </div>
</div>



<h2 class="text-center my-4 text-primary">Hospitals</h2>

<div class="container">
  <div class="row">
    <div class="col-md-12">
      <div class="table-responsive">
        <table class="table table-bordered table-striped table-hover">
          <thead class="thead-dark">
            <tr>
              <th>Name</th>
              <th>Location</th>
              <th>Email</th>
              <th>License No</th>
              <th class="text-center">Actions</th>
            </tr>
          </thead>
          <tbody>
            <% @hospitals.each do |hospital| %>
              <tr>
                <td><%= hospital.name %></td>
                <td><%= hospital.location %></td>
                <td><%= hospital.email %></td>
                <td><%= hospital.license_no %></td>
                <td class="">
                  <%= link_to "http://#{hospital.subdomain}.localhost:3000/users/sign_in", class: 'btn btn-primary btn-sm me-2 ', rel: 'noopener noreferrer' do %>
                    Go to hospital
                  <% end %>

                  <%# <%= link_to edit_hospital_path(hospital), class: 'btn btn-success btn-sm me-2 ' do %> 
                    <%# <i class="bi bi-pencil"></i>  %>
                  <%# <% end %> 

                  <%# <%= button_to hospital, method: :delete, data: { confirm: 'Are you sure you want to delete this hospital?' }, class: 'btn btn-danger btn-sm ' do %> 
                    <%# <i class="bi bi-trash"></i>  %>
                  <%# <% end %> 
                </td>

              </tr>
            <% end %>
          </tbody>

        </table>
 <%= will_paginate @collection, renderer: BootstrapPagination::Rails %>
      </div>
    </div>
  </div>
</div>
