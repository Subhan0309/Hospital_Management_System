<%= render partial: 'shared/table_view', locals: {
  title: 'Patients',
  collection: @patients,
  headers: ['Name', 'Email', 'Gender','View Details'],
  fields: ['name', 'email', 'gender'],
  resource_class: Patient,
  new_path: new_patient_path,
  edit_path: ->(item) { edit_patient_path(item) },
  destroy_path: ->(item) { patient_path(item) },
  show_medical_record_path: -> (item) {user_medical_records_path(item)},
  show_appointments_path: ->(item) {user_appointments_path(item)}
} %>
